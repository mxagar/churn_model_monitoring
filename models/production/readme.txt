After `deployment.py`is run, this folder should contain:

- `trained_model.pkl`: trained model for production
- `ingested_files.csv`: records of data files ingested to train the model
- `latest_score.csv`: score records of the trained model 